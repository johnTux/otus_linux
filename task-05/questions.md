# Вопросы к домашнему заданию №5 "Управление процессами"

## Вопрос №1

Хочу реализовать:

> "1) написать свою реализацию ps ax используя анализ /proc"

Для этого вывожу список всех pid-директорий в /proc.
```
#!/usr/bin/env bash

PROC=/proc

ls -a $PROC | grep -o '[0-9]*' | sort -n
```